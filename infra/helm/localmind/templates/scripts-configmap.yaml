apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "localmind.fullname" . }}-scripts
  labels:
    app.kubernetes.io/name: {{ include "localmind.name" . }}
data:
  pull_models.sh: |
{{ .Files.Get "scripts/pull_models.sh" | indent 4 }}
